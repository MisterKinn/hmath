# Minimal requirements without pyhwpx
# Use this for testing the GUI on non-Windows systems or if pyhwpx fails

PySide6>=6.6
Pillow>=10.0
pdf2image>=1.17.0
pytesseract>=0.3.10
opencv-python>=4.10.0
numpy>=1.26
pyinstaller>=6.0
watchdog

# Install pyhwpx separately on Windows with Hancom Hangul:
# pip install pyhwpx>=1.6.6
